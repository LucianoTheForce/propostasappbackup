export function Grid() {
  return (
    <div className="fixed inset-0 z-0 pointer-events-none overflow-hidden">
      {/* Removemos as linhas de grade para um design mais limpo e sólido */}
    </div>
  )
}
